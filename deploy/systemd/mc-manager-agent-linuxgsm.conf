[Service]
# LinuxGSM stores its scripts and game files in its service user's home.
ProtectHome=read-only
ReadWritePaths=/home/mcserver

# LinuxGSM uses a tmux socket in /tmp. The agent and normal LinuxGSM commands
# must share the same /tmp namespace or they cannot see the same server session.
PrivateTmp=false
